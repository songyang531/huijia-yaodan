# Copy to .env.local for local Next.js. All values are server-only.
# Demo mode works without any key. Only enable live extraction in a trusted demo environment.
ENABLE_LIVE_AI=false
OPENAI_API_KEY=
# A vision-capable Responses model with Structured Outputs, available to your account.
OPENAI_MODEL=
